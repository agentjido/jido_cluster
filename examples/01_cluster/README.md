# 01 Cluster

Start with [01_01 Keyed counter](01_01_keyed_counter/README.md). It shows one logical counter accessed from two Erlang nodes. It then stops the owner and restores the saved state on the surviving node.

Read the Agent and the mirrored tests in that order. Later examples can add more cluster concepts without expanding this first example.

Return to the [catalog](../README.md).
